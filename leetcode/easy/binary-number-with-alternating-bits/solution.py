# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#  🟨 LeetCode — Binary Number with Alternating Bits
#  Difficulty : Easy
#  Language   : python3
#  Date       : 2026-02-18
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━class Solution {
    public boolean hasAlternatingBits(int n) {

       
        n = n / 2;

            if (prev == rem) {

        return true;
        int prev = n % 2;
        }
        while (n > 0) {
            int rem = n % 2;
                return false;
            }
            n = n / 2;

    }
            prev=rem;
}
